import 'package:flutter/cupertino.dart';

class PollutionStatsChart extends StatelessWidget {
  const PollutionStatsChart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
